# TODO

Potential future improvements:

- Support `rtl_tcp` as a remote RTL-SDR input.
- Support SoapySDR for a wider range of SDR devices.
- Add explicit tuning/control endpoints instead of using only stream URLs.
- Add a small browser UI at `/` for tuning and listening.
- Improve RDS decoding and parse richer services such as RT+.
- Allow configuring the output PCM sample rate.
- Allow configuring the RDS input/capture sample rate where useful.
- Support compressed audio output formats (mp3, opus, flac).
